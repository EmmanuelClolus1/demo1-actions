name: Workflow local
on: [push,workflow_dispatch]
jobs:
    buildJob:
        runs-on: ubuntu-latest
        steps:
          - name: Simulation d'éxecution du Build
            run: echo "Build en cours"
    testJob:
        runs-on: ubuntu-test
        steps:
          - name: Simulation d'éxecution du test
            run: echo "Test en cours"